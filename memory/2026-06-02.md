# Memory — 2026-06-02

## Session: Windows 11 QEMU/KVM VM + WinRE Recovery Partition

### Work completed today

- **apply-winre-recovery.ps1** — PowerShell script that creates a recovery partition at the end of C: (disk-final position), copies an already-extracted winre.wim, registers it with reagentc, then hides the drive letter. Includes confirmation prompt, duplicate-partition guard, optional C: shrink.
- **apply-winre-recovery-guide.md** — full guide for the script
- **apply-winre-recovery-onepage.md** — one-page customer-delivery summary
- **apply-winre-recovery-onepage.html** — HTML version (used for PDF generation via browser)
- **apply-winre-recovery-onepage.pdf** — PDF generated via Chrome CDP (saved to media/browser directory)

### Key decisions

- Recovery partition placement: **C: 뒤, 디스크 마지막** (after C:, end of disk) — confirmed by user, standard Windows layout
- Layout: `[EFI] -> [MSR] -> [Windows(C:)] -> [Recovery]`
- Recovery GUID: `de94bba4-06d1-4d40-a16a-bfd50179d6ac`
- GPT attributes: `0x8000000000000001`

### Bugs fixed

1. **`??` null-coalescing operator** — PowerShell 5.1 incompatible; replaced with `if` branch
2. **`$RecoveryVolume = "$DriveLetter:`"` string termination error** — replaced with `$DriveLetter + ':'`
3. **`winre-extract.ps1` encoding corruption** — rewrote file clean from scratch in UTF-8

### Git state

- Repo: `github.com:ozun215-lab/Claw.git`, branch `main`
- Latest commit: `9fcc7c7` — "fix: PS5 compat - remove null-coalescing op, fix RecoveryVolume string"
- Pending (not committed): `scripts/install-backup-and-push-task.ps1` — TaskName changed from `'Claw Backup And Push'` to `'Claw Sync'`
- git binary: `C:\Program Files\Git\cmd\git.exe` (not in PATH by default in this shell)

### Script parameters reference

```
apply-winre-recovery.ps1
  -WinReWimPath   <path to winre.wim>   (mandatory)
  -RecoverySizeMB <int, default 1024>
  -DriveLetter    <single letter, default R>
  -ShrinkOSDrive  (switch)
  -Force          (switch, skip confirmation)
```

### Work directory

`D:\Claw\workspace\Window11-VM\`

### Customer execution command

```powershell
Set-ExecutionPolicy Bypass -Scope Process -Force
.\apply-winre-recovery.ps1 -WinReWimPath C:\Temp\WinRE\winre.wim -ShrinkOSDrive
```
